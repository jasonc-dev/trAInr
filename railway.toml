# Railway configuration for multi-service deployment
# This file tells Railway how to deploy each service

[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile.multi"

[deploy]
startCommand = "docker-compose up"
healthcheckPath = "/"
healthcheckTimeout = 300